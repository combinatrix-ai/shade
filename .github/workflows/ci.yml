name: CI
on: [push, pull_request]
permissions:
  contents: read
jobs:
  check:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/check.sh
      - run: bash -n scripts/release.sh
